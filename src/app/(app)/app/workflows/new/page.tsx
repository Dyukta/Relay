import { CreateWorkflowPage } from "@/features/workflows/components/CreateWorkflowPage"

export const metadata = {
  title: "Create Workflow — Relay",
}

export default function Page() {
  return <CreateWorkflowPage />
}