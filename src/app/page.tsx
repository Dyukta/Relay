import { LandingPage } from "@/features/landing/components/LandingPage"

export const metadata = {
  title: "Relay ",
  description:
    "Receive events through webhooks, process them asynchronously and monitor every execution in real time.",
}

export default function Page() {
  return <LandingPage />
}